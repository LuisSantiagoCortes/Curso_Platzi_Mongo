use("sample_training")

db.zips.updateMany(
    // filtro del Id que se quiere acutalizar
    {city : "CLEVELAND"},
    
    // update operators 
    // $inc incrementa el 100 a la variable pop
    {$inc : {
        pop : 1000
    }
    }
)