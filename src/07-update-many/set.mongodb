use("sample_training")

db.zips.updateMany(
    // filtro del Id que se quiere acutalizar
    {city : "CLEVELAND"},
    
    // update operators 
    {$set : {
        myAttr : "Hola ssantiago"
    }
    }
)